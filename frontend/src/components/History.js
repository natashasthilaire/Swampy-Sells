import React, {useState} from "react";
import Header from "./Header";
import '../App.css'

export const History = (props) => {
    
    return (
        <div>
          <Header /> 
        </div>
    )
}